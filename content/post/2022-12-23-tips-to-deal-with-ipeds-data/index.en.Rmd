---
title: IPEDS data cleaning in R
author: Shiya Liu
date: '2022-12-23'
slug: tips-to-deal-with-ipeds-data
categories:
  - R
  - IPEDS
tags:
  - IPEDS
subtitle: ''
summary: 'IPEDS data cleaning in R'
authors: []
lastmod: '2022-12-23T16:25:09-08:00'
featured: no
image:
  caption: ''
  focal_point: ''
  preview_only: no
projects: []
active: false
draft: true
---
```{r setup, include=FALSE}
knitr::opts_chunk$set(eval = FALSE)
```

```{r}
library(tidyverse)
```

# Merge multiple state-level dataframes by state and year
```{r}
merge_cols <- c("State","Year")
df <- Reduce(function(x,y) merge(x,y, by = merge_cols), list(df1, df2, df3, df4,...))
```

# Replace state full name with state abbreviations

```{r}
x <- unique(DF$State)
DF[[i]]$State <- state.abb[match(x,state.name)]
```

```{r}
DF$State <- state.abb[match(DF$state,state.name)]
```

